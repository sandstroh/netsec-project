server {
    listen 80;

    location /cgi-bin {
        root /var/www/static;
        gzip off;
        fastcgi_pass unix:/var/run/fcgiwrap.socket;
        include /etc/nginx/fastcgi_params;
        fastcgi_param $SCRIPT_FILENAME $document_root$fastcgi_script_name;
    }

    location / {
        try_files $uri/cgi-bin/page.html =404;
    }
}
